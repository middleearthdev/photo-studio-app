version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: studio_foto_db
    environment:
      POSTGRES_DB: studio_foto_app
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data: